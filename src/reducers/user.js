const initialState = {
  userId: 'luke_skywalker', // TODO Remove hack and assign user id
}

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
